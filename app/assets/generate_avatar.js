// This is a simple script to generate a default avatar image
// You can run this with Node.js if you need to generate more avatars
// For now, we're using a static image from the Expo template

console.log('Default avatar is already generated');
